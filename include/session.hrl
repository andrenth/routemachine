-record(session, {
  establishment,
  server,
  rib,
  local_asn,
  remote_asn,
  local_addr,
  remote_addr,
  hold_time,
  hold_timer,
  keepalive_time,
  keepalive_timer,
  conn_retry_time,
  conn_retry_timer
}).
